# OAuth Server URL (where the OAuth server is running)
OAUTH_SERVER_URL=http://localhost:3001

# Transport mode: 'stdio' (default, for Claude Desktop) or 'http' (for ChatGPT via ngrok)
MCP_TRANSPORT=stdio

# Port for HTTP transport mode (only used when MCP_TRANSPORT=http)
MCP_PORT=3002
