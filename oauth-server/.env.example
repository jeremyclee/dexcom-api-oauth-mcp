# Dexcom API Credentials
DEXCOM_CLIENT_ID=your_client_id_here
DEXCOM_CLIENT_SECRET=your_client_secret_here
DEXCOM_REDIRECT_URI=http://localhost:3001/auth/callback

# Environment: 'sandbox', 'production' (US), 'production_eu' (Europe/Canada), 'production_jp' (Japan)
DEXCOM_ENV=sandbox
# Optional: Override base URL directly (leave commented to use default based on DEXCOM_ENV)
# DEXCOM_API_BASE_URL=https://sandbox-api.dexcom.com

# Server Configuration
PORT=3001
NODE_ENV=development

# Security - Generate a random 32-character string
TOKEN_ENCRYPTION_KEY=change_this_to_a_random_32_char_key
